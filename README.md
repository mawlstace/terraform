# terraform

Simple Terraform IaaC template to provision a single server in availability zone a, with a
20GB operating system root volume, and 1GB locally attached data volume.The
server will be used to calculate complex equations.
